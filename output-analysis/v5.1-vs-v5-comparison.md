# Spec convergence — v5 → v5.1

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v5 | v5.1 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 3 | 1 | -2 | ✅ |
| Edge cases | 4 | 1 | -3 | ✅ |
| Ambiguities | 3 | 1 | -2 | ✅ |
| R coverage — full | 49 | 49 | 0 | ➖ |
| R coverage — partial | 0 | 0 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 7 | 7 | 0 | ➖ |
| Autofixes applied (detail-review) | 0 | 0 | 0 | ➖ |
| Flagged (detail-review) | 5 | 2 | -3 | ✅ |
| Conforming (audit) | 21 | 21 | 0 | ➖ |
| Marginal (audit) | 1 | 1 | 0 | ➖ |
| Non-conforming (audit) | 0 | 0 | 0 | ➖ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **16** | **6** | **-10** | **✅** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full
  / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

Total issues = Gaps + Edge cases + Ambiguities + R coverage
(partial + gap) + Flagged + Marginal + Non-conforming +
Fetch-failed.

## Verdict

**ON TRACK**

Total issues dropped 16 → 6 (Δ = -10). The minor refinement
closed every spec-text-addressable issue surfaced by v5's three
analyses; the residual 6 issues are all context-level decisions
(or naming-policy decisions) tracked in v5.1's Refinement gaps
section. No category regressed; no new gaps, edge cases, or
ambiguities surfaced from the v5.1 deltas.

## Per-category notes

- **Gaps -2** (3 → 1). v5.1 closed G-1 (R14 anchor explicit
  cross-reference in §1.2). v5 G-2 and G-3 remain — both
  context-level (form tie-break vocabulary; click-tracking
  lifecycle vs trim). Tracked as Refinement gaps row 1 (G-3 → v5.1
  G-1; the form-tie-break gap was deferred via Refinement gaps
  row 1 in the spec).
- **Edge cases -3** (4 → 1). v5.1 closed EC-3 (§4.6 pre-roll
  non-linear Player behaviour now specified) and EC-4 (§5.2.2
  non-zero `<Period>@duration` now specified as E2). v5 EC-2
  (malformed click URL) was host-environment dependent and
  deferred. v5 EC-1 (overlay longer than linear) remains because
  the missing UC-04 walkthrough is a `context/` deliverable.
- **Ambiguities -2** (3 → 1). v5.1 closed A-1 (dual carrier
  beacon-set is single-rooted, §5.5.4) and A-3 (pause-ad
  pre-fetch staleness was non-normative; nothing addressable in a
  minor refinement — deferred). The remaining v5.1 A-1 is the
  `@priority` DP-1.2 redundancy, deferred as a context-level
  decision.
- **Flagged -3** (5 → 2). v5.1 resolved v5 flag-2 (§5.1.4 NOTE),
  flag-3 (§5.4.1 N06.13 citation), and flag-4 (§4.4.8 ↔ §7.6
  refactor). v5 flag-1 (Ticks suffix policy) and flag-5
  (`<EventStream>` reinterpretation) remain — both naming /
  architectural decisions for context.
- **Marginal unchanged** (1 → 1). C-11 (`<EventStream>` reinterpreted
  inside `<svta:RenderableAsset>`) carries forward because the
  remedy is a context-level DR-8 admission or a rename, neither
  of which a minor refinement can apply.
- **Non-conforming unchanged** (0 → 0). v5 had already closed the
  v4.2 N-1 via the zero-duration-Period envelope; v5.1 preserves
  that closure and adds the E2 tightening (§5.2.2) so an authoring
  violation is now an explicit Player error condition rather than
  unspecified.

## Convergence recommendation

The Phase 1 spec is at a stable resting point. The 6 residual
issues each require either:
- a `context/03-requirements.md` change (G-1 form tie-break,
  G-3 / EC-2 click semantics, A-1 `@priority` semantics),
- a `context/04-use-cases.md` change (EC-1 UC-04 walkthrough),
- a `context/06-naming-and-namespaces.md` change (flag-1 Ticks
  suffix policy), or
- a `context/08-dash-extension-rules.md` change (flag-5 / M-1
  DR-8 candidate).

None of these are spec-text refinements. Continuing with vN.M+2
without a context edit would produce no net convergence — the
remaining 6 items would carry forward unchanged. The next
productive iteration is either (a) a context update followed by
a major build vN+1, or (b) ratify v5.1 as the Phase 1 deliverable
and escalate the open items to the working group.
