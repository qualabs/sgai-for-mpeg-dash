# Spec convergence — v6 → v7

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v6 | v7 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 8 | 8 | 0 | ➖ |
| Edge cases | 7 | 7 | 0 | ➖ |
| Ambiguities | 2 | 2 | 0 | ➖ |
| R coverage — full | 11 | 11 | 0 | ➖ |
| R coverage — partial | 1 | 1 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 3 | 3 | 0 | ➖ |
| Autofixes applied (detail-review) | 0 | 0 | 0 | ➖ |
| Flagged (detail-review) | 2 | 1 | -1 | ✅ |
| Conforming (audit) | 22 | 22 | 0 | ➖ |
| Marginal (audit) | 5 | 5 | 0 | ➖ |
| Non-conforming (audit) | 1 | 1 | 0 | ➖ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **26** | **25** | **-1** | **✅** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

## Verdict

**ON TRACK**

Total issues decreased from 26 to 25 (−1). The reduction comes
entirely from the detail-review flagged surface: v6 flagged item
#2 (OQ "Origin in this spec" column uniformity) was closed in v7
by an editorial sweep across all twelve OQ rows. The remaining v6
flag (item #1: OQ placeholder issue numbers, a carry-over of v5
flag-2) was deferred per its own recommendation (gated on external
GitHub state). The v7 detail-review pass surfaced no new flag — the
micro-consistency scan across the eight categories yielded zero
new findings, and the v7-detail-review document carries only the
deferred-by-design carry-over. Net: 2 → 1 flagged (-1).

The gap / edge-case / ambiguity surface is byte-identical: v7
applied no edits that touch the normative chapters in substance.
The eight gaps (G-1..G-8), seven edge cases (EC-1..EC-7), and two
ambiguities (A-1, A-2) carry forward from v6 unchanged. The
fifteen-row Refinement gaps section in the spec body is byte-
identical between v6 and v7. R coverage is unchanged (11 full, 1
partial R13, 3 governance R8/R9/R10).

The conformance-audit picture is byte-identical across both runs
(22 conforming, 5 marginal, 1 non-conforming, 0 fetch-failed) —
v7 introduced no new XML constructs, so the construct inventory
(C-01..C-28) carries forward unchanged. The OQ column
normalisation in v7 is conformance-neutral (it edits prose inside
an informative section; no XML construct is touched). The
structural items that gate further convergence (R13 partial
coverage; N-1 zero-`<Period>` overlay resolution doc; M-2
`@earliestResolutionTimeOffset` rename) all still require
`context/` changes outside minor scope, and v7 deliberately
stayed inside spec-only scope.

## Per-category notes

- **Gaps unchanged (8 → 8)**: v7 deliberately did not address
  gap-class issues. The eight gaps (G-1..G-8) carry from v6
  byte-for-byte. The recommended context refinements section
  (R13.2 split, pause-ad dismissal MUST,
  `@earliestResolutionTimeOffset` rename decision, UC-03+UC-05
  composition, foreign-namespace timing reinterpretation
  authorisation) is unchanged.
- **Edge cases unchanged (7 → 7)**: same — v7 did not address
  edge-case-class issues.
- **Ambiguities unchanged (2 → 2)**: same.
- **R coverage unchanged**: 11 full, 1 partial (R13), 3
  governance (R8, R9, R10).
- **Autofixes unchanged (0 → 0)**: expected. v7's delta surface
  is a single editorial sweep across twelve table cells; each
  cell required a semantic decision (which implication clause
  paraphrases the open question best; which short qualifier
  categorises the process-item row), so no autofix-eligible
  mechanical inconsistencies were present.
- **Flagged -1 (2 → 1)**: convergence signal. v6 flag #2 (OQ
  column uniformity) closed by the v7 normalisation pass —
  every cell now uses one of two consistent templates (design
  vs process). v6 flag #1 (OQ placeholder issue numbers,
  carried from v5 flag-2) deferred per its own recommendation
  and re-emitted as v7 flagged item #1 with no expected closure
  until the GitHub issues are created. The v7 detail-review
  surfaced no new flagged item. Net 2 → 1.
- **Conformance audit unchanged (22 / 5 / 1)**: v7 introduces no
  new XML constructs; the construct inventory (C-01..C-28) is
  byte-identical to v6. The OQ column normalisation touches no
  XML construct (it modifies prose inside an informative
  section), so no audit inventory changed. No new conformance
  findings, no escalations.
- **OQ section in v7**: the OQ-1..OQ-12 forward-looking
  informative material carries forward with one v7 delta (column
  normalisation). The "Topic" column and the "Open question"
  column are byte-identical to v6. The "Origin in this spec"
  column now uses two consistent templates across all twelve
  rows. The validator continues NOT to count OQ-N items as gaps
  or ambiguities.

The verdict is **ON TRACK**: v7 closed the last remaining minor-
eligible editorial flag from v6, did not regress on any
structural dimension, and confirmed that the structural surface
the minor-refinement envelope can touch is now drained. The
residual issues (8 gaps, 7 edge cases, 2 ambiguities, 1
non-conforming, 4 marginal) are the same set v6 carried, all
flagged in the Refinement gaps section of the spec body, and all
require either a context-level change or a WG decision before
they can be lifted.

## Convergence trajectory and runway

Convergence trajectory (v4.2 → v5 → v6 → v7): 29 → 27 → 26 → 25
total issues. The slope is flattening as expected: ∆ = -2, -1, -1.
The minor-refinement envelope has now reached its terminal point.
After v7:

- The only remaining detail-review flag (OQ placeholder issue
  numbers) is gated on external GitHub state. No agent-local
  editorial sweep can close it.
- All eight gaps, seven edge cases, and two ambiguities require
  context-level changes (the five recommended refinements
  carried over from v5).
- The conformance audit's non-conforming (N-1) and marginal
  (M-1, M-2, M-4, M-5) items are all on the Refinement-gaps
  list, all requiring context-level or WG decisions.
- No new XML construct can be introduced inside a minor
  iteration; that path is reserved for a major build.

**Runway for minor-only refinement after v7**: zero. The next
forward motion of any kind requires either (a) one or more of
the five context-level refinements landing in `context/`, (b)
WG decisions on 2026-06-02 that fold OQ-N items into the
normative chapters via a major build, or (c) the GitHub issues
being created to unblock the placeholder-issue-numbers caveat
removal (the only remaining editorial sweep, but gated externally).

Recommended next step is one of:

1. **Stop iterating on Phase 1 minor refinements.** v7 is the
   stable terminal point of the minor-only refinement track.
   Subsequent iterations of any substance require context-level
   work or WG input. No v8 minor refinement is meaningful.
2. **Wait for WG decisions on 2026-06-02** and escalate to a
   major build that folds the resolved OQ-N items into the
   normative chapters. The expected scope depends on which OQs
   the WG decides (e.g. OQ-1 IAB-by-reference tightens R10.2
   with explicit mechanism; OQ-4 `DynamicPresentation` adoption
   touches §5.1).
3. **Address the context-level blockers in parallel** (split
   R13.2, decide rename vs admit-NOTE for
   `@earliestResolutionTimeOffset`, etc.). These unblock G-1,
   G-7, G-8 / A-2 and feed into the next major build regardless
   of the WG decisions.
