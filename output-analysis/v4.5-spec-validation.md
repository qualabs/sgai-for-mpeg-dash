[GROUNDED_BY=spec-only]

# Spec validation — v7 (2026-05-20)

Built against:
- spec: `../output/v7-sgai-spec.md` (2463 lines)
- context/ at git SHA: `e3f3939` (working tree clean re context/ — `context/` not modified by v7)
- analysis inputs: `../context-analysis/dash-gap-analysis.md`,
  `../context-analysis/conformance-assertions.md`,
  `../context-analysis/error-semantics.md`,
  `../context-analysis/uc-coverage-matrix.md`,
  `../context-analysis/iab-ad-templates.md`
- prior analyses (delta basis): `../output-analysis/v6-spec-validation.md`,
  `../output-analysis/v6-detail-review.md`,
  `../output-analysis/v6-dash-conformance-audit.md`
- carry-over external input: `sandbox/sgai-dash-if-2026-05-19-analysis.md`
  (DASH-IF WG meeting), `sandbox/sgai-dash-if-2026-05-19-issues-proposal.md`
  (12-issue proposal). No new external input in v7.

Mode: independent peer review by a fresh subagent. No NotebookLM
grounding requests issued; validation compares the v7 spec text
against `context/` and `context-analysis/` as the authoritative
source.

Delta against v6 (non-normative, for reviewer orientation).
v7 is a minor-style editorial delta on top of v6 (the spec file
inherits v6 byte-for-byte for every section not enumerated below).
The numbered increments are:

- **"Open questions for WG (2026-06-02 meeting)" section, "Origin
  in this spec" column across all twelve OQ rows** — closes v6
  detail-review flagged item #2 (column uniformity). v6's column
  varied in information density: some cells carried only spec
  refs (e.g. OQ-1 / OQ-2 / OQ-4 / OQ-6 / OQ-7 / OQ-8 / OQ-12),
  one carried refs plus an implication clause (OQ-3, after v6's
  flag-1 fix), and the three process items (OQ-9 / OQ-10 / OQ-11)
  used inconsistent qualifiers (`n/a (process item)` vs `n/a
  (governance item)`). v7 normalises the column to two consistent
  templates:
  - Design-item OQs (OQ-1..OQ-8, OQ-12): `<refs> (the open
    question, if admitted/resolved, would <implication>)`. OQ-3
    is unchanged byte-for-byte because it already matched this
    template after v6's flag-1 closure.
  - Process-item OQs (OQ-9, OQ-10, OQ-11): `n/a — <qualifier>`
    where qualifier is one of `coordination item` / `governance
    item` / `paper output item`. The em-dash separator replaces
    v6's parenthetical `(process item)` / `(governance item)`.
  Annotation: `<!-- refine: v6-detail-review.md#flag-2 -->` placed
  immediately above the table header.

- **Version-line bump** — `v6 -> v7` at the top metadata block.
  Annotation: `<!-- refine: version bump v6 -> v7 -->`.

That is the entire v7 delta surface. No other section was touched.

NOT applied in v7 (deliberate):
- **v6 detail-review flagged item #1** (OQ placeholder issue
  numbers, carried over from v5 flag-2). The flag's suggested
  resolution is "defer until the issues are created in
  `qualabs/sgai-for-mpeg-dash`". The issues are not yet created;
  deferral persists. The "placeholders" qualifier remains in the
  OQ section's preamble. This is not a regression — it is the
  explicit deferral the flag itself recommends.

All five recommended context refinements from v4.2 / v5 / v6
carry forward unresolved — v7 deliberately stayed inside spec-only
scope and did not attempt context-level changes. The five
recommendations are the ones flagged by validation as requiring
context/ edits or WG decisions:

1. Split R13.2 into ADS MUST and Player MUST (closes G-1 / A-1).
2. Define Player obligation to dismiss `<svta:PauseAdPresentation>`
   on viewer resume (closes G-3).
3. Decide rename vs admit-NOTE for `@earliestResolutionTimeOffset`
   (closes G-7).
4. Compose UC-03 and UC-05 (closes G-4 / EC-4).
5. Admit "baseline element under SVTA parent with reinterpreted
   timing" pattern in `context/08-dash-extension-rules.md`
   (closes G-8 / A-2).

v7 introduces no new gaps, edge cases, or ambiguities of its own
that require requirement-level remediation. The Open-questions-for-WG
section retains its informative status; the column-uniformity
normalisation is purely editorial — every implication clause added
is a plain-language restatement of what the corresponding open
question already asks, drawn verbatim from the same row's "Open
question" cell, with no new substantive claim introduced.

---

## Gaps (8)

All eight gaps carry forward from v6 unchanged. v7's delta surface
(OQ "Origin in this spec" column normalisation, version bump) did
not move the gap surface.

### G-1. No normative ADS obligation to author non-linear beacon `presentationTime` in the slot-window time base

Carried from v6 unchanged. Refinement-gaps row 3.

### G-2. Click-through interaction is defined but the trigger event is not

Carried from v6 unchanged. Refinement-gaps row 4.

### G-3. Pause-ad dismissal on viewer resume is informal

Carried from v6 unchanged. Refinement-gaps row 5.

### G-4. Pause inside an active `<svta:OverlayPresentation>` is unspecified

Carried from v6 unchanged. Refinement-gaps row 6.

### G-5. Tracking-only ad (no `<MediaFile>`) lacks a normative carrier

Carried from v6 unchanged. Refinement-gaps row 7.

### G-6. R8 per-construct justification audit table is not consolidated

Carried from v6 unchanged. Refinement-gaps row 8.

### G-7. SGAI `@earliestResolutionTimeOffset` keeps a colliding name with `<ImportedMPD>` despite context/06 mandating a rename

Carried from v6 unchanged. Refinement-gaps row 2.

### G-8. `<svta:RenderableAsset>`-child `<EventStream>` reinterprets baseline timing without a context-level authorisation

Carried from v6 unchanged. Refinement-gaps row 15.

---

## Edge cases (7)

All seven edge cases carry forward from v6 unchanged. v7 did
not address edge-case-class issues.

### EC-1. `allowedLayouts` populated entirely with inadmissible tokens

Carried from v6 unchanged. Refinement-gaps row 9.

### EC-2. Overlay window exceeds linear window in a hybrid slot

Carried from v6 unchanged. Refinement-gaps row 10.

### EC-3. Every candidate semantically empty after E8 filtering routes through E6

Carried from v6 unchanged. Refinement-gaps row 11.

### EC-4. Pause inside an active overlay window

Carried from v6 unchanged. Same root cause as G-4 /
Refinement-gaps row 6.

### EC-5. `maxDuration=0` on a slot

Carried from v6 unchanged. Refinement-gaps row 12.

### EC-6. ADS resolution returns the wrong document profile for the slot type

Carried from v6 unchanged. Refinement-gaps row 13.

### EC-7. Sub-MPD `<Period>@duration` vs `<svta:RenderableAsset @duration>` mismatch (video form)

Carried from v6 unchanged. Refinement-gaps row 14.

---

## Ambiguities (2)

Both v6 ambiguities carry forward; v7 introduces none.

### A-1. ADS responsibility to remap non-linear `presentationTime` is implied but not codified

Carried from v6 unchanged. Linked to G-1.

### A-2. `<svta:RenderableAsset>` reinterprets baseline `<EventStream>` semantics without context authorisation

Carried from v6 unchanged. Linked to G-8 / Refinement-gaps row 15.

---

## R coverage map

| R   | Status     | Spec sections that satisfy it | Notes |
|-----|------------|-------------------------------|-------|
| R1  | full       | §1, §4.1, §4.4.1, §4.4.8, §4.5, §5.1.3 / §5.1.4 legacy-Player paragraphs, §5.6.4, §7.6 (legacy row), §8.1 E5, Annex G | Unchanged from v6. |
| R2  | full       | §4.1 three-actor model, §4.2 B-1..B-7, §4.3 A-1..A-7, §4.4.1..§4.4.8, §7 algorithm | Unchanged from v6. |
| R3  | full       | §3.4 D1..D5 taxonomy, §7.6 per-device matrix, Annexes A.5/B.5/C.5/D.4/E.4/F.5 | Unchanged from v6. OQ-8 (capability discovery vocabulary) remains a forward-looking refinement of R3. |
| R4  | full       | §4.2 B-2, §4.4.3, §4.4.6 trim-stops-beacons, §5.1.3 / §5.1.4 / §5.2.1 / §5.2.2, §7.3 step 4.c / step 5, §8.1 E9 / E10 | Unchanged from v6. |
| R5  | full       | §4.4.2 / §4.4.5, §5.3, §7.3 step 5 (1)-(4) | Unchanged from v6. |
| R6  | full       | §4.3 A-4, §5.5, §6.5, §7.5 | Unchanged from v6. |
| R7  | full       | §4.4.4, §7.3 step 4 (linear) / step 5 (non-linear), §8.1 E9 | Unchanged from v6. |
| R8  | governance | Inline justifications scattered across §5.1.3, §5.1.4, §5.2.2, §5.5, §5.6 | Unchanged from v6 — see G-6 (no consolidated audit table). |
| R9  | governance | §5.1.1 / §5.1.2 / §5.4 / §5.5 / §5.2.2 / §7.4 — heavy baseline reuse | Unchanged from v6. |
| R10 | governance | §1.1 OOS, §3.2 IAB-derived layout vocabulary, §7.4 | Unchanged from v6. OQ-1 still surfaces an active WG tension; spec preserves R10 as written. |
| R11 | full       | §1 scope, §4.3 ADS section, §6.6, Annex H | Unchanged from v6. |
| R12 | full       | §3.2, §4.2 B-3, §4.3 A-5 | Unchanged from v6. |
| R13 | partial    | §4.4.6, §5.5.2 / §5.5.4 | Unchanged from v6 — see G-1 / A-1; R13 stays partial because the ADS leg is missing in `context/03-requirements.md` R13.2. |
| R14 | full       | §1.1 OOS, §4.2 B-7, §4.4.7, §5.1.3 | Unchanged from v6. OQ-2 still surfaces a WG question about relaxing R14; spec preserves R14 as written. |
| R15 | full       | §1.1 OOS-4, §3.2, §3.1, §4.3 A-3, §5.3.2, §5.3.4 | Unchanged from v6. |

Breakdown: **11 full** (R1..R7, R11, R12, R14, R15), **1 partial**
(R13), **3 governance** (R8, R9, R10) = 15 of 15. Unchanged from v6.

---

## Recommended context refinements

The five recommendations from v5 / v6 carry forward unresolved —
v7 deliberately stayed inside spec-only scope and did not attempt
context-level changes. They are listed below for continuity:

### 1. Split R13.2 into ADS MUST and Player MUST

Carried unchanged. High-leverage; closes G-1 and A-1; unblocks R13
to full coverage.

### 2. Define the Player obligation to dismiss `<svta:PauseAdPresentation>` on viewer resume

Carried unchanged. Closes G-3.

### 3. Decide rename vs admit-NOTE for `@earliestResolutionTimeOffset`

Carried unchanged. Closes G-7.

### 4. Compose UC-03 and UC-05 (pause during active overlay)

Carried unchanged. Closes G-4 / EC-4.

### 5. Admit the "baseline element under SVTA parent with reinterpreted timing" pattern in `context/08-dash-extension-rules.md`

Carried unchanged. Closes G-8 / A-2.

---

## Open questions surfaced by v7 (informative only)

The Open-questions-for-WG section carries forward from v6 with one
v7 delta (column normalisation across all twelve rows). The delta
is purely editorial — every implication clause added to a design-
item OQ restates the open question already present in the row's
"Open question" cell using a uniform `<refs> (the open question,
if admitted/resolved, would <implication>)` template; every
process-item OQ now uses `n/a — <qualifier>` uniformly. No
substantive content was added or removed.

- The placeholder-issue-numbers caveat is retained in the
  section's preamble (deferred per v5 flag-2 / v6 flagged item
  #1's own recommendation, until the GitHub issues are created).
- The lifecycle framing paragraph added in v6 is unchanged.
- OQ-1..OQ-12 cell content in columns "Topic" and "Open question"
  is byte-identical between v6 and v7.

The validator does NOT report the OQ section as gaps or
ambiguities in the counts above. The reasoning is the same as v6:
OQ-N items are externally surfaced design questions, not gaps in
the current spec's internal coverage of `context/`. The spec
preserves the current `context/03-requirements.md` formulations
(R10, R14, R3) faithfully.

Validator's read on v7: **safe and appropriate** for a minor
editorial iteration. The single delta applied is sub-paragraph in
scope and explicitly traces to v6 detail-review flagged item #2;
no normative chapter content changed in v7, no construct surface
moved, no R coverage shifted.

---

## Coverage map note (counts in summary)

- Gaps: 8 (carried)
- Edge cases: 7 (carried)
- Ambiguities: 2 (carried)
- R-coverage: 11 full / 1 partial / 3 governance (carried)
- New gaps in v7: 0
- New edge cases in v7: 0
- New ambiguities in v7: 0
- Net structural delta: 0

The structural surface that minor-refinement can touch is fully
drained after v7. The remaining residual issues (8 gaps, 7 edge
cases, 2 ambiguities, 1 non-conforming, 4 marginal in the audit)
all require either a context-level change or a WG decision before
they can be lifted, and v7 confirms this in two ways: (a) no new
minor-eligible issue surfaced, and (b) the last identified minor-
eligible editorial sweep from v6 has now landed.
