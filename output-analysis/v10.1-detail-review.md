[GROUNDED_BY=spec-only]

# Spec detail review — v10.1 (2026-09-25)

**Review target**: `../output/v10.1-sgai-spec.md`.
**Reference**: `../context/` at git SHA `22ae6f831541ffddbef7c6e96556764eeadf92ae`
(`git status --porcelain context/` is empty).
**Method**: deterministic scan across eight micro-consistency categories;
autofixes applied in place, flags reported here.

## What this pass could and could not decide

**Grounding is spec-only.** The primary copy of the base standard was not
opened, by instruction, so categories 3 and 4 are bounded to what the spec
says about its own reuses. v10.1 is a minor refine of v10: it introduces no
element, attribute, URI or token. The `svta:` element inventory of every
example is still the 11 that chapter 5 tables and §5.10 declares.

**Every scan ran over the whole spec, not only over the refine's delta**
(`diff v10 v10.1`: 497 changed lines, 110 `<!-- refine: -->` markers). Every
negative result below comes from an instrument that was first made to fail
(§"Instrument controls").

**Items the validation sidecar already routes are not re-flagged.** Three of
them fall inside this review's categories and would otherwise appear here:

- T2: the "strictly" of §5.10 L3192-3193 against the lax wildcard.
- T3: test X-5 requiring `@layout` on every option.
- T4: "the decision's order (§4.4)" at L3701 and L8149.

## Summary

- **Autofixes applied: 0.** The spec is byte-identical to its state before
  this pass (`cmp` against a pre-pass copy: identical; sha256
  `9832bd4b…faea54`, 8 858 lines, 469 751 B).
- **Issues flagged for human decision: 2.**
- **v10's two flags are resolved in v10.1**:
  - flag-1: the D4 document of M.5 L7646-7697 is now line-identical to I.3
    L6537-6586 once the refine comments are dropped (`difflib`: no diff), and
    the elision text and the four fragments name the metadata.
  - flag-2: §3.6 L575 now names `squeezeback-double-box` (without background).
- **v10's five autofix classes held in v10.1**:
  - Q.7.1, Q.7.2 at L1314;
  - the B-10 wording at L8756;
  - six `<Reporting>` sites;
  - no `§X, item N` comma form;
  - every fragment root declares `xmlns:svta`.
- Categories with no issues:
  - **cross-reference consistency.** All internal `§` refs resolve, excluding
    the ones that cite the base standard (0 unresolved).
    - 107 `§X item/step N` refs, none past its section's last item. No
      section changed its item count between v10 and v10.1.
    - Every `§4.4 item N` still names the item it meant.
    - The 28 `R<n>` ids cited all exist in `context/03-requirements.md`.
    - No test id is cited outside Annex Q.
  - **schema-namespace pinning.** 87 XML blocks. 86 parse; the one that does
    not is the §5.3.5 two-root elision at L2439, as in v10.
  - **annex vs chapter mismatch.** 0 violations of the chapter-5 rules across
    the examples. That includes the rule the refine changed:
    - The one List MPD option (A.3 L3911) carries neither `@layout` nor
      `@duration`, as §5.3.1 L2273 and §5.3.3 L2353 now require.
    - Every non-linear option carries both.
  - **TODO / FIXME / placeholder leftovers.** No marker. The four
    `placeholder` hits (L1365, L1441, L2904, L2948) are prose about a
    placeholder *value*. The trailing section is the subject of flag 2.
  - **naming reuse with different semantics / same-name alignment.** Nothing
    new; v10.1 adds no name.
  - **format consistency (other).**
    - Codec strings are lowercase (`avc1.4d401f`, `avc1.64001f`,
      `avc1.640028`, `mp4a.40.2`).
    - Annexes A–P carry `*This annex is informative.*` (16 of 17, as in v10;
      Annex Q is the test annex).
    - `---` still precedes every chapter and annex.

## Autofixes applied

None. No finding met the autofix bar. The one class that is mechanical in
form — adding a modal to a list item — is flagged, because it changes
normative force.

## Flagged issues

| # | Category | Spec ref | Rule anchor | Description | Suggested resolution | Recommendation |
|---|---|---|---|---|---|---|
| 1 | editorial regression (chapter vs its own reading rule) | §4.2 items 3 L663, 4 L667, 7 L676, 11 L685; §4.3 item 3 L708; §4.4 items 1 L724, 3 L731 | §4.1 L605-606: *"Each obligation is stated positively, with its RFC 2119 keyword"*; the sibling items of the same lists, which the refine rewrote as `MUST <verb>` (e.g. §4.2 items 1, 2, 5, 6, 10) | Seven lead items state an obligation in the indicative with no RFC 2119 keyword anywhere in the item: *"Authors each opportunity…"*, *"Declares `@id`, `@presentationTime` and `@duration`…"*, *"Declares `@customRegion` only…"*, *"Authors every SGAI construct so that…"*, *"Declares the tracking schedule…"*, *"Exposes the endpoint…"*, *"Produces a document that validates…"*. The same numbered list therefore mixes "MUST declare" and "Declares". They are **not** among T1's five sites, because T1 covers only items with a `context/` MUST behind them. The sidecar judged the rest of its 22 to be definitions or permissions, and these seven are neither. | Rewrite each lead verb as `MUST <verb>`, as the refine did for the siblings. §4.4 item 5 is already T1. | Add the modals. All seven are actor obligations under §4.1's own rule, and the list reads as two conventions. Flagged, not fixed: a modal changes force, and whether the seven lack a `context/` counterpart is the validation's call. |
| 2 | TODO / placeholder leftovers (drafting section) | "Refinement gaps", L8819-8858 | Category 8 (drafting markers); CLAUDE.md, `dist/` holds what the project stands behind | The candidate ends with a 31-row "Refinement gaps" table ("carried forward for the next major build to address"). It is not in the table of contents (L25-51), and it cites v10 sidecar ids (`5.b-B1`…`5.c-C27`). Some of its reasons are already stale against v10.1: for example, row 17 says the spec sites "keep the indicative". It is required by `refine-spec.prompt` §4, so it is correct in the candidate, but it is build bookkeeping and not specification text. | Keep it in `output/`. At promotion, strip it from the published spec, or move it to `output-analysis/v10.1-…`. | Strip at promotion. Flagged, not fixed: removing it would break the refine contract, and deciding what gets published is not this step's call. |

## Open questions surfaced

- **T4 has a third, uncited sibling.** O.5 L8235-8236, *"The survivors keep
  the order the decision gave them"*, makes the same claim as L3701 and L8149
  but cites no section. It narrates what this APS does rather than attributing
  a rule to §4.4, so it is not a broken reference. Whoever applies T4 should
  decide whether it stays.
- **The refine left long unwrapped source lines.** There are 23 prose lines
  over 80 characters, excluding tables, code and the refine comments; v10 had
  15. Examples: L795, L813, L3036 (115 characters), and L1302, where the
  sentence continues after the refine comment. This has no effect on the
  rendered output and is not fixed; it disappears when the next major build
  regenerates the text.

## Instrument controls

Each scan was run on v10.1 as found, and on a copy with one defect of its own
class injected. A scan that reads the same on both cannot fail; none did.

| Scan | Injected defect | As found | Injected |
|---|---|---|---|
| C1 unresolved internal `§` refs (the base-standard prefix is looked for on the previous line too) | `(§4.5.4)` → `(§4.5.44)` (25 sites) | 0 | **25** |
| C2 XML block that does not parse / unbound prefix | `xmlns:svta` dropped from the first block declaring it | 1 (§5.3.5 elision) | **2** |
| C3 chapter-5 rules on every XML example | comma in `@allowedLayouts` | 0 | **2** |
| C3 | `form="image"` → `form="gif"` | 0 | **1** |
| C3 | `@onCandidatesExhausted` removed from a pause document | 0 | **2** |
| C3 | `layout="linear"` added to the A.3 List MPD option | 0 | **1** |
| C3 | `<Reporting>` renamed in the §5.9 example | 0 | **1** |
| C3 | callback `@value="1"` → `"2"` | 0 | **1** |
| C3 | uppercase hex in a `@codecs` | 0 | **1** |
| C3 | `<svta:Click>` moved before `<svta:RenderableAsset>` in a candidate | 0 | **1** |
| C4 `§X item N` past the section's last item | `§4.5.1 item 3` → `item 9` (L8683) | 0 | **1** |
| C5 drafting markers | a `TODO: settle this.` line | 0 | **1** |
| C6 modal-less chapter-4 items (flag 1's list) | — (it reproduces T1's five sites among its 19 hits, which is its positive control) | 19 | — |

A first run of C3 matched `<Metrics>` only in the base namespace. It
therefore missed un-namespaced fragments, so the `<Reporting>` control read 0.
The scan was changed to match local names, and every C3 figure above is from
the corrected version.

## What was not touched

- Nothing under `../context/`: `git status --porcelain context/` is empty.
- Nothing under `../context-analysis/`. The five modified files there were
  modified before this pass started.
- `../output-analysis/v10.1-spec-validation.md`, Step 7's artefact.
- `../output/v10.1-sgai-spec.md` was not edited: `cmp` against the copy taken
  before the scans reports identical.
