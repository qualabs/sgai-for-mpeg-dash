[GROUNDED_BY=spec-only]

# Spec detail review — v6.1 (2026-05-28)

**Review target**: `../output/v6.1-sgai-spec.md`.
**Reference**: `../context/` at git SHA `d30c655`.
**Method**: deterministic scan across eight micro-consistency
categories; autofixes applied in place, flags reported here.

## Summary

- Autofixes applied: 6
- Issues flagged for human decision: 3
- Categories with no issues: (3) naming reuse with different
  semantics, (4) same-name same-semantics alignment,
  (7) schema-namespace pinning, (8) TODO/FIXME/placeholder
  leftovers

Top categories with autofixes applied: (6) editorial regression —
the v6-detail-review.md#flag-3 codec-contrast NOTE landed in v6.1
with two identical literals (the validation sidecar's G-5),
mechanically corrected here; (2) cross-reference consistency —
two broken section pointers repaired; (1) format consistency —
three XML examples re-aligned where the v6-detail-review
`<svta:EventStream>` → `<EventStream>` rename left a 4-column
hangover on continuation-attribute lines.

The v6.1 spec inherits the v6 spec's clean state for the
remaining categories — no namespace-pinning regressions appeared
in the new NOTEs and skeletons added by the refinement; no naming
or annex-vs-chapter mismatches survived the refinement; the
codec-casing convention announced by flag-3 is honoured by every
annex example.

## Autofixes applied

| # | Category | Spec ref | Rule anchor | Before | After |
|---|----------|----------|-------------|--------|-------|
| 1 | (6) editorial regression | §5.4 codec NOTE, L:1206 | spec-internal: the NOTE's surrounding prose pins "lowercase hexadecimal in the codec identifier per IETF RFC 6381 §3" and "case-sensitive, and RFC 6381 recommends lowercase"; the contrast literals MUST be lowercase-vs-uppercase, not identical. Tracks v6.1 validation G-5. | `` (`avc1.4d401f`, not `avc1.4d401f`) `` | `` (`avc1.4d401f`, not `avc1.4D401F`) `` |
| 2 | (2) cross-reference consistency | §4.3 Publisher obligations, L:512 | spec-internal: §5.1.3.2 is "Layout token semantics" (covers only `@allowedLayouts`); `@maxConcurrency` is declared in §5.1.3.1 (the `<svta:OverlayPresentation>` attributes table) and the table row carries the "explicit declaration of the single-active-form contract" rationale the sentence cites. §8.7 (L:1972) already uses `(§5.1.3.1)` for the same attribute — consistent precedent. Off-by-one-digit typo. | `the one-active-form runtime contract; §5.1.3.2).` | `the one-active-form runtime contract; §5.1.3.1).` |
| 3 | (2) cross-reference consistency | §7.5.5 Pause-triggered ad, L:1743 | spec-internal: §5.1.3 (the `<svta:OverlayPresentation>` chapter) and its four subsections (§5.1.3.1..§5.1.3.4) do not discuss in-slot sequencing; §4.6.6 "Single-active-form rendering" is the only section that defines the rule the sentence cites ("When the resolution document declares multiple non-linear forms for the same slot, the Player presents them in document order, each starting when the previous one ends"). | `back-to-back (§5.1.3 in-slot sequencing), each fullscreen.` | `back-to-back (§4.6.6 in-slot sequencing), each fullscreen.` |
| 4 | (1) format consistency — whitespace | §5.2.2.1 skeleton, L:1012–1013 | spec-internal: continuation-attribute lines align to the column of the first attribute on the opening tag (the convention used by every other multi-line `<EventStream>` block — e.g. §5.5.1 L:1240–1241, Annex A.4 L:2077–2078). v6 detail-review autofix #3 renamed `<svta:EventStream>` → `<EventStream>` without dropping the now-extra 4 columns on continuation lines; v6.1's NC-1 wrap preserved the regression. | `<EventStream xmlns:cb=...` opens at col 22; `schemeIdUri=...` continuation at col 26 (4 cols too deep) | continuation re-aligned to col 22 (drop 4 spaces) |
| 5 | (1) format consistency — whitespace | Annex C.3 callback EventStream, L:2308–2309 | same anchor as #4 | same misalignment | same fix (4 spaces dropped) |
| 6 | (1) format consistency — whitespace | Annex E.3 callback EventStream, L:2507–2508 | same anchor as #4 | same misalignment | same fix (4 spaces dropped) |

## Flagged issues

| # | Category | Spec ref | Rule anchor | Description | Suggested resolution | Recommendation |
|---|----------|----------|-------------|-------------|----------------------|----------------|
| 1 | (3) naming reuse with different semantics (carried from v6) | §5.1.1 L:781, §5.1.2 L:824, §5.1.3.1 L:847, §5.1.4.1 L:892 (timescale-unit flavour) vs §5.2.1.3 L:960 and §5.3.4 L:1110 (seconds flavour) | `context/06-naming-and-namespaces.md` §"Naming consistency with baseline DASH" requires reusing a name with all its characteristics including units | v6.1 added a NOTE at §5.1.1 (`<!-- refine: v6-detail-review.md#flag-1 -->`, L:783–799) documenting the two unit conventions on the same `@earliestResolutionTimeOffset` attribute name. The NOTE is well-placed but the underlying collision (one attribute name, two unit bases) is unchanged — the v6 detail-review flag-1 is mitigated, not closed. A validator still cannot tell the two flavours apart from the attribute alone. | Either (a) rename one flavour (e.g. `@earliestResolutionTimeTicks` on the timescale-units side or `@earliestResolutionTimeSeconds` on the seconds side); (b) split into two distinct attribute names; or (c) keep the NOTE as the canonical disambiguation and accept the validator gap. | Carry forward as flag-only — the NOTE is a defensible v6.1 mitigation; final rename is a `context/` decision out of detail-review scope. |
| 2 | (2) cross-reference consistency — ambiguous target | (potential category, not applied) | — | The §4.3 ref at L:512 (autofix #2 above) was borderline between §5.1.3.1 (attribute definition) and §4.6.6 (runtime contract). The autofix picked §5.1.3.1 by the surrounding-sentence test ("see also" pattern points to the attribute's defining row, mirroring §8.7 L:1972's `(§5.1.3.1)` precedent). Documented here so a future reviewer can revisit. | If on re-read the authorial intent is to point at the runtime contract, change L:512 to `§4.6.6`. Otherwise leave the autofix as is. | Flag-only — the autofix is defensible; the alternative target is also coherent. |
| 3 | (5/6) annex vs chapter — Annex K.1 footnote anchors for E10 / E11 (carried from v6) | Annex K.1 rows TC-E10 (L:2945) and TC-E11 (L:2947) | spec-internal: v6.1 applied the v6-detail-review flag-4 refine, moving the rule anchor from `§4.6.4` to `§4.6.5` on both rows. The operative language ("skips the candidate") lives in §4.6.5; §4.6.4 covers ordering preservation. The refine is in place; no further edit needed. Logged as evidence-of-resolution rather than open issue. | None — confirmed closed. | Documented closed; no action. |

## Open questions surfaced

- The `xmlns:cb="urn:mpeg:dash:event:callback:2015"` declaration on §5.2.2.1 skeleton (L:1011), §5.5.1 carrier shape (L:1240), Annex C.3 (L:2307), and Annex E.3 (L:2506) is never used by any `cb:`-prefixed element in the example body. v6 detail-review's "Open questions" already noted this as harmless noise out of category 7 scope (the rule pins declarations of namespaces that ARE used, not pruning unused ones). Carried forward unchanged — a future minor refinement may want to drop the four declarations.
