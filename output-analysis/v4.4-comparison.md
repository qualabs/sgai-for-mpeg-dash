# Spec convergence — v5 → v6

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v5 | v6 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 8 | 8 | 0 | ➖ |
| Edge cases | 7 | 7 | 0 | ➖ |
| Ambiguities | 2 | 2 | 0 | ➖ |
| R coverage — full | 11 | 11 | 0 | ➖ |
| R coverage — partial | 1 | 1 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 3 | 3 | 0 | ➖ |
| Autofixes applied (detail-review) | 0 | 0 | 0 | ➖ |
| Flagged (detail-review) | 3 | 2 | -1 | ✅ |
| Conforming (audit) | 22 | 22 | 0 | ➖ |
| Marginal (audit) | 5 | 5 | 0 | ➖ |
| Non-conforming (audit) | 1 | 1 | 0 | ➖ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **27** | **26** | **-1** | **✅** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

## Verdict

**ON TRACK**

Total issues decreased from 27 to 26 (−1). The reduction comes
from the detail-review flagged surface: two of v5's three flags
(flag-1 OQ-3 origin-cell wording; flag-3 §1.1 enhancement-layer
OOS bullet sharpening) were closed in v6 by surgical prose edits
(annotated `<!-- refine: v5-detail-review.md#flag-1 -->` and
`<!-- refine: v5-detail-review.md#flag-3 -->`); the third flag
(flag-2 OQ placeholder-issue numbers) was deferred per its own
recommendation (the v5 flag explicitly recommended "defer until
the issues are created in `qualabs/sgai-for-mpeg-dash`"), and the
v6 detail-review pass surfaced one new stylistic flag continuing
v5's "Open questions surfaced" note about OQ column uniformity
(v5 marked it as MAY-normalise, not a flag; v6 promotes it to
the flag table to make the residual editorial work explicit).
Net: 3 → 2 flagged (-1).

The gap / edge-case / ambiguity surface is byte-identical: v6
applied no edits that touch the normative chapters in substance.
The eight gaps (G-1..G-8), seven edge cases (EC-1..EC-7), and two
ambiguities (A-1, A-2) carry forward from v5 unchanged. The
fifteen-row Refinement gaps section in the spec body is byte-
identical between v5 and v6. R coverage is unchanged (11 full, 1
partial R13, 3 governance R8/R9/R10).

The conformance-audit picture is byte-identical across both runs
(22 conforming, 5 marginal, 1 non-conforming, 0 fetch-failed) —
v6 introduced no new XML constructs, so the construct inventory
(C-01..C-28) carries forward unchanged. The §1.1 enhancement-layer
OOS bullet rewrite in v6 is conformance-neutral (it makes the
§5.3.2 / §5.3.4 admissibility consistent with the §1.1 OOS
framing — admitting a Broadcaster's right to author a `video` form
whose sub-MPD carries a scalable-codec Representation, which was
already a pre-existing degree of freedom under §5.3.4 / RFC 4337).
The structural items that gate further convergence (R13 partial
coverage; N-1 zero-`<Period>` overlay resolution doc; M-2
`@earliestResolutionTimeOffset` rename) all still require
`context/` changes outside minor scope, and v6 deliberately
stayed inside spec-only scope.

## Per-category notes

- **Gaps unchanged (8 → 8)**: v6 deliberately did not address
  gap-class issues. The eight gaps (G-1..G-8) carry from v5
  byte-for-byte. The recommended context refinements section
  (R13.2 split, pause-ad dismissal MUST, `@earliestResolutionTimeOffset`
  rename decision, UC-03+UC-05 composition, foreign-namespace
  timing reinterpretation authorisation) is unchanged.
- **Edge cases unchanged (7 → 7)**: same — v6 did not address
  edge-case-class issues.
- **Ambiguities unchanged (2 → 2)**: same.
- **R coverage unchanged**: 11 full, 1 partial (R13), 3
  governance (R8, R9, R10). The §1.1 enhancement-layer OOS
  bullet rewrite tightens the R15 boundary at the wording level
  but does not move R15's coverage status.
- **Autofixes unchanged (0 → 0)**: expected. v6's delta surface
  is too small and too purposeful (each of the three substantive
  edits required a semantic decision — which option from flag-3's
  resolution to apply, which lifecycle wording to introduce) to
  contain autofix-eligible mechanical inconsistencies.
- **Flagged -1 (3 → 2)**: convergence signal. v5 flags closed:
  flag-1 (OQ-3 cell rewording) and flag-3 (§1.1 OOS bullet
  tightening), both via inline edits with `<!-- refine: ... -->`
  annotations. v5 flag-2 (OQ placeholder-issue numbers) was
  deferred per its own recommendation and re-emitted as v6
  flagged item #1 with no expected closure until the GitHub
  issues are created. The v6 detail-review surfaced one new
  flagged item — flagged item #2 — about OQ column uniformity,
  promoting v5's "MAY normalise" stylistic note into the flag
  table (v5 had not counted it as a flag). Net 3 → 2.
- **Conformance audit unchanged (22 / 5 / 1)**: v6 introduces no
  new XML constructs; the construct inventory (C-01..C-28) is
  byte-identical to v5. The §1.1 enhancement-layer OOS bullet
  rewrite touches no XML construct (it modifies an OOS deferral
  in §1.1, which is non-normative scope text), so no audit
  inventory changed. No new conformance findings, no escalations.
- **OQ section in v6**: the OQ-1..OQ-12 forward-looking
  informative material carries forward with two minor v6 deltas
  (OQ-3 cell rewording from flag-1; new lifecycle framing
  paragraph addressing v5 detail-review's first "Open questions
  surfaced" note). Neither delta is normative. The validator
  continues NOT to count OQ-N items as gaps or ambiguities.

The verdict is **ON TRACK**: v6 closed two of v5's three flagged
editorial issues, added one informative lifecycle paragraph that
explicitly preserves the minor-refinement contract (no normative
change), and did not regress on any structural dimension. The
residual issues (8 gaps, 7 edge cases, 2 ambiguities, 1
non-conforming, 4 marginal) are the same set v5 carried, all
flagged in the Refinement gaps section of the spec body, and all
require either a context-level change or a WG decision before
they can be lifted.

Convergence trajectory (v4.2 → v5 → v6): 29 → 27 → 26 total
issues. The slope flattens (∆=-2, -1) as the structural surface
the minor-refinement envelope can touch drains. Next minor
refinement (v6.1) has at most a sub-30-minute editorial sweep
available (OQ "Origin in this spec" column normalisation) before
the only remaining path forward is a context-level change or a WG
decision. The convergence is real but the runway for minor-only
refinement is short.

Recommended next step is one of:

1. **Continue minor refinement (v7)** to apply the OQ column
   uniformity normalisation flagged in v6 detail-review (sub-30
   minute edit; lands 2 → 1 on the flagged dimension). Marginal
   value; defers the structural blockers another iteration.
2. **Wait for WG decisions on 2026-06-02** and escalate to a
   major build that folds the resolved OQ-N items into the
   normative chapters. The expected scope depends on which OQs
   the WG decides (e.g. OQ-1 IAB-by-reference tightens R10.2 with
   explicit mechanism; OQ-4 `DynamicPresentation` adoption
   touches §5.1).
3. **Address the context-level blockers in parallel** (split
   R13.2, decide rename vs admit-NOTE for
   `@earliestResolutionTimeOffset`, etc.). These unblock G-1,
   G-7, G-8 / A-2 and feed into the next major build regardless
   of the WG decisions.
