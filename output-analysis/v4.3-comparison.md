# Spec convergence — v4.2 → v5

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v4.2 | v5 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 8 | 8 | 0 | ➖ |
| Edge cases | 7 | 7 | 0 | ➖ |
| Ambiguities | 2 | 2 | 0 | ➖ |
| R coverage — full | 11 | 11 | 0 | ➖ |
| R coverage — partial | 1 | 1 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 3 | 3 | 0 | ➖ |
| Autofixes applied (detail-review) | 2 | 0 | -2 | ⚠️ |
| Flagged (detail-review) | 5 | 3 | -2 | ✅ |
| Conforming (audit) | 22 | 22 | 0 | ➖ |
| Marginal (audit) | 5 | 5 | 0 | ➖ |
| Non-conforming (audit) | 1 | 1 | 0 | ➖ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **29** | **27** | **-2** | **✅** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

## Verdict

**ON TRACK**

Total issues decreased from 29 to 27 (−2). The reduction comes
from the detail-review flagged surface: all five v4.2 flags
(flag-1 image-row dangling cross-ref; flag-2 ERT cross-ref to
§5.1.1; flag-3 VAST cross-ref to "annexes"; flag-4 §2.1 omits
§5.1.2; flag-5 §4.3 ADS-MAY bullet conflating spec-defined SVTA
carriers with vendor-namespaced) were silently resolved in the v5
refine pass, and the v5 detail-review pass surfaced three new
finer-grained flags (one OQ-3 origin-cell ambiguity, one
placeholder-issue-numbers note, one enhancement-layer OOS bullet
sharpening) — net −2 on the flagged dimension.

The autofix count dropped from 2 to 0 because v5's delta surface
(cross-reference fixes already applied as refine deltas, plus the
additive §1.1 OOS bullet and Open-questions-for-WG section)
introduced no new mechanical inconsistencies; the autofix decline
reflects a cleaner starting point, not a regression. The trend
marker shows ⚠️ because the prompt treats "autofixes applied" as
a good category (more = better); in this context the lower count
is expected and signals convergence, not stalling.

The conformance-audit picture is byte-identical across both runs
(22 conforming, 5 marginal, 1 non-conforming, 0 fetch-failed) —
v5 introduced no new XML constructs, so the inventory carries
forward unchanged. The structural items that gate further
convergence (R13 partial coverage; N-1 zero-`<Period>` overlay
resolution doc; M-2 `@earliestResolutionTimeOffset` rename) all
still require `context/` changes outside minor scope, and v5
deliberately stayed inside spec-only scope.

## Per-category notes

- **Gaps unchanged (8 → 8)**: v5 deliberately did not address
  gap-class issues. The eight gaps (G-1..G-8) carry from v4.2
  byte-for-byte. The recommended context refinements section
  (R13 split, pause-ad dismissal MUST, `@earliestResolutionTimeOffset`
  rename decision, UC-03+UC-05 composition, foreign-namespace
  timing reinterpretation authorisation) is unchanged.
- **Edge cases unchanged (7 → 7)**: same — v5 did not address
  edge-case-class issues.
- **Ambiguities unchanged (2 → 2)**: same.
- **R coverage unchanged**: 11 full, 1 partial (R13), 3
  governance (R8, R9, R10). The new §1.1 enhancement-layer OOS
  bullet sharpens the R15 boundary; the open-questions section is
  informative and does not move R10 / R14 coverage status (the
  spec preserves the current `context/` formulations even though
  WG discussion may amend them).
- **Autofixes -2 (2 → 0)**: expected. The v4.2 autofix slack (two
  mechanical fixes: §3.3 OOH cross-reference and §3.1 misplaced-
  parent slip) was already cleaned in v4.2. v5's delta surface
  contains no mechanical fix opportunities — the cross-reference
  remediations applied in v5 are flag-derived (carrying explicit
  refine annotations), not autofix-eligible. The dimension is
  drained.
- **Flagged -2 (5 → 3)**: convergence signal. The v5 detail-review
  pass surfaced three new issues all editorial / forward-looking
  in nature (OQ-3 origin-cell wording; placeholder issue numbers;
  enhancement-layer OOS bullet sharpening). None is structural;
  all three are fixable in another minor pass.
- **Conformance audit unchanged (22 / 5 / 1)**: v5 introduces no
  new XML constructs; the construct inventory (C-01..C-28) is
  byte-identical to v4.2. The §2.1 cross-reference fix reinforces
  the §5.1.2 anchoring of `<ReplacePresentation>` without
  changing the M-1 verdict. The §4.3 ADS-MAY bullet rewrite
  tightens placement of `<svta:Click>` / `<svta:UniversalAdId>`
  but does not introduce new constructs. No new conformance
  findings, no escalations.
- **New material the comparison does NOT count**: the
  Open-questions-for-WG section with OQ-1..OQ-12 is informative
  forward-looking material. The validator explicitly chose not to
  fold these into the gap / ambiguity dimension because they are
  externally surfaced design questions, not gaps in current
  coverage. If the WG resolves any of OQ-1..OQ-12 in the
  2026-06-02 meeting, the next major build (v6) will fold the
  decision into the normative chapters and the comparison-vs-v5
  will register the additions in the appropriate dimension at
  that time.

The verdict is **ON TRACK**: v5 closed v4.2's five flagged
editorial issues, added forward-looking informative material that
explicitly preserves the minor-refinement contract (no normative
change), and did not regress on any structural dimension. The
residual issues (8 gaps, 7 edge cases, 2 ambiguities, 1
non-conforming, 4 marginal) are the same set v4.2 carried, all
flagged in the Refinement gaps section of the spec body, and all
require either a context-level change or a WG decision before
they can be lifted.

Recommended next step is one of:

1. **Continue minor refinement (v5.1)** to clean the three
   detail-review flags surfaced in this pass (OQ-3 origin cell
   wording, placeholder issue numbers once the GitHub issues are
   created, enhancement-layer OOS bullet sharpening). All three
   are sub-1-hour edits and would land 5 → 0 on the flagged
   dimension.
2. **Wait for WG decisions on 2026-06-02** and escalate to a
   major build (v6) that folds the resolved OQ-N items into the
   normative chapters. The expected scope of v6 depends on which
   OQs the WG decides (e.g. if OQ-1 is decided IAB-by-reference,
   it tightens R10.2 with explicit mechanism; if OQ-4 is decided
   to add `DynamicPresentation`, it touches §5.1).
3. **Address the context-level blockers in parallel** (split
   R13.2, decide rename vs admit-NOTE for
   `@earliestResolutionTimeOffset`, etc.) — these unblock G-1,
   G-7, G-8 / A-2 and feed into v6's major build regardless of
   the WG decisions.

## Top 3 mejoras pendientes (input for v6 if applied)

1. **OQ-1 / OQ-2 resolution** (Issues #2 / #3 in the proposal):
   WG decision on layout system (DASH-native vs IAB-by-reference)
   and on R14 (single non-linear form vs multi-overlay). These
   two are the highest-leverage decisions for v6 because they
   touch the chapter-5 syntax surface directly.
2. **Context refinements 1, 3, 5** from this validation (split
   R13.2; decide
   `@earliestResolutionTimeOffset` rename; admit
   foreign-namespace timing reinterpretation in
   `context/08-dash-extension-rules.md`). These three close G-1 /
   A-1, G-7, and G-8 / A-2 simultaneously — unblock R13 to full,
   and remove the Refinement-gaps rows 2, 3, 15.
3. **OQ-4 / OQ-5 / OQ-7** (Issues #5 / #6 / #10): the
   `DynamicPresentation` event family. If the WG adopts it, v6
   introduces a new chapter-5 §5.1.x section and a new
   capability-discovery sub-section in §3 or §4. If the WG
   rejects it, v6 records the decision via ADR and tightens §5.1
   to make the current Insert/Replace split more explicit. Either
   resolution is a v6-major-build-grade change.
