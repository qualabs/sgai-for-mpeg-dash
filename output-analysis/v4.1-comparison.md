# Spec convergence — v4 → v4.1

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v4 | v4.1 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 8 | 10 | +2 | ⚠️ |
| Edge cases | 7 | 7 | 0 | ➖ |
| Ambiguities | 5 | 2 | -3 | ✅ |
| R coverage — full | 11 | 11 | 0 | ➖ |
| R coverage — partial | 1 | 1 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 3 | 3 | 0 | ➖ |
| Autofixes applied (detail-review) | 4 | 6 | +2 | ✅ |
| Flagged (detail-review) | 5 | 3 | -2 | ✅ |
| Conforming (audit) | 20 | 22 | +2 | ✅ |
| Marginal (audit) | 4 | 5 | +1 | ⚠️ |
| Non-conforming (audit) | 3 | 1 | -2 | ✅ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **33** | **29** | **-4** | **✅** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full
  / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

## Verdict

ON TRACK

- Total issues decreased from 33 to 29 (Δ = -4). The refinement
  resolved two of the three v4 Non-conforming items (N-2, N-3) and
  one Marginal item (M-3), and closed three of five v4
  Ambiguities (A-2, A-3, A-4).
- Two categories ticked up and are worth flagging — see per-category
  notes below — but neither offsets the headline drop.

## Per-category notes

- **Gaps +2 (8 → 10)**: the v4.1 spec-validation sidecar re-numbered
  the gap list and surfaced two new items the refinement itself
  introduced (G-9: Annex A.2 `xsi:schemaLocation` without `xmlns:xsi`
  declaration; G-10: undocumented `@returnOffset` / `@startWithOffset`
  provenance on `<ReplacePresentation>`). G-9 was subsequently
  cleared by detail-review autofix #6; G-10 is a documentation gap
  the next iteration should backfill with a §5.16.4 citation.
- **Non-conforming -2 (3 → 1)**: the highest-impact movement. N-2
  (`@clipDuration` schema/type mismatch) and N-3 (UrlParamInfo
  signalling pattern) are both resolved. Only N-1 (zero-`<Period>`
  Overlay Resolution Document) remains — explicitly deferred to a
  major build per Refinement-gaps row 1.
- **Marginal +1 (4 → 5)**: M-5 surfaced as a side-effect of the N-3
  autofix — `<UrlParamInfo>` vs `<RequestParam>` element-name
  authority in DASH 6th itself (schema-vs-prose split, same
  pattern as M-1). Audit recommends a M-1-style inline NOTE; small
  enough for a future minor.
- **Ambiguities -3 (5 → 2)**: A-2 (`allowedLayouts` matching),
  A-3 (`@mediaType` vs `@mimeType`), and A-4 (one-candidate
  non-linear rule) all closed by surgical edits in §5.1.3, §3.1,
  and §4.4.4. A-1 (ADS-side timing obligation) and A-5 (baseline
  element under SVTA parent) require context changes and stay
  open.
- **R coverage unchanged**: R13 stays partial because the gap
  requires an R13 split in `context/03-requirements.md` — outside
  minor scope. The minor refinement deliberately did not attempt
  context-level changes.
