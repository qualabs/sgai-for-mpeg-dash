[GROUNDED_BY=spec-only]

# Spec detail review — v6 (2026-05-20)

**Review target**: `../output/v6-sgai-spec.md`.
**Reference**: `../context/` at git SHA `e3f3939` (working tree
clean re context/ — `context/` not modified by v6).
**Method**: deterministic scan across eight micro-consistency
categories; autofixes applied in place where the right answer is
unambiguous, flags reported here. Findings already captured by
`../output-analysis/v6-spec-validation.md` (the eight gaps,
seven edge cases, two ambiguities, and G-7 naming-collision on
`@earliestResolutionTimeOffset`) are not restated here. The three
v5 detail-review flags (flag-1, flag-2, flag-3) are reviewed for
closure status below.

## Summary

- Autofixes applied: 0
- Issues flagged for human decision: 2
- Categories with no issues: format consistency (cat 1 — no slip);
  TODO / FIXME / placeholder leftovers (cat 8 — the OQ
  "placeholders until creation" qualifier is the only one, and it
  is intentional per v5 flag-2's own deferral recommendation);
  naming reuse with different semantics (cat 3 — the
  `@earliestResolutionTimeOffset` collision is captured by
  validation G-7 and is on the refinement-gaps list, not re-
  flagged here); annex vs chapter mismatch (cat 5 — no annex was
  touched in v6); editorial regression on the chapter-vs-annex
  axis (cat 6 — every v6 edit is in non-annex sections and does
  not propagate to annexes); schema-namespace pinning (cat 7 — no
  XML example was touched).

## v5 flag closure status

| v5 flag | Subject | v6 status | Notes |
|---------|---------|-----------|-------|
| flag-1 | OQ-3 origin-cell wording | **CLOSED** | v6 rewrites the cell from `§4.1 three-actor model bypassed for non-ad content` to `§4.1 three-actor model (the open question, if admitted, would bypass the ADS round-trip for non-ad concurrent content)`. The new wording is unambiguous about which part is current spec state vs proposed change. |
| flag-2 | OQ placeholder issue numbers | **DEFERRED** (per flag-2's own recommendation) | The flag explicitly recommended "defer until the issues are created in `qualabs/sgai-for-mpeg-dash`". The issues are not yet created; deferral persists. Re-emitted below as a continuing-deferral note (not a new flag). |
| flag-3 | §1.1 enhancement-layer OOS bullet vs §3.2 / §5.3.2 video-form admissibility | **CLOSED** | v6 rewrites the bullet to apply option (a) from flag-3's suggested resolution: MUST NOT define a normative construct that ties enhancement-layer Representations to primary base layers; a Broadcaster authoring a `video` form whose sub-MPD carries a scalable codec is conformant. The §5.3.2 / §5.3.4 admissibility surface is now consistent with the §1.1 OOS framing. |

## Autofixes applied

None. v6's delta surface (one OOS bullet rewrite, one OQ cell
rewording, one new lifecycle paragraph, one version bump) introduced
no new mechanical inconsistencies that an autofix would correct. The
three flagged-applied edits were not autofix candidates — each
required a semantic decision (which option from the v5 flag's
"suggested resolution" to apply, which wording the new lifecycle
paragraph carries), so the prompt routes them through the
delta-applied annotated edits (`<!-- refine: v5-detail-review.md#... -->`)
and not the autofix log.

## Flagged issues

| # | Category | Spec ref | Rule anchor | Description | Suggested resolution | Recommendation |
|---|----------|----------|-------------|-------------|----------------------|----------------|
| 1 | cross-reference consistency (cat 2) | "Open questions for WG (2026-06-02 meeting)" section, every OQ-N cell mentioning an Issue # | spec-internal: the OQ table maps OQ-1..OQ-12 to GitHub Issues #2..#13. The spec body still acknowledges the issues "are placeholders until creation". A reader following the issue # without context still cannot tell whether the issue actually exists. (Carried-forward continuation of v5 flag-2.) | Once the issues are created in `qualabs/sgai-for-mpeg-dash`, update the section to (a) remove the "placeholders" qualifier and (b) optionally turn each issue # into a hyperlink to the issue URL. | Continue deferring until the issues are created. Apply a minor refinement once the GitHub state is stable. No action required in v6.x without GitHub state change. |
| 2 | uniformity (cat 4) | "Open questions for WG (2026-06-02 meeting)" section, "Origin in this spec" column across all 12 rows | spec-internal: the column varies in information density. Some cells point at a specific subsection (e.g. OQ-1's `§1.1 layout OOS bullet; §3.2 layout vocabulary`); others are abstract (OQ-9 / OQ-10 / OQ-11 say `n/a (process item)`). The v5 detail-review observed this and marked it MAY-normalise. (Carried-forward continuation of v5 "Open questions surfaced" stylistic note 2.) | Normalise the column to a consistent template: for design-item OQs (OQ-1..OQ-8, OQ-12) list spec sections + the open question's implication; for process-item OQs (OQ-9, OQ-10, OQ-11) keep `n/a (process item)` and add a short qualifier (e.g. `n/a — coordination`, `n/a — governance`, `n/a — paper output`). | Defer. Sub-30-minute edit. Apply in a future minor pass alongside other editorial sweeps. The current heterogeneity is correctness-equivalent. |

## Open questions surfaced

- The new lifecycle paragraph added to the OQ section in v6
  ("This section is transient. It records the state of WG-surfaced
  design questions as of the 2026-05-19 meeting and is removed
  from the spec in the major build that follows the WG meeting
  where each OQ resolves...") settles the lifecycle question
  raised by v5 detail-review's first stylistic note. A future
  edition will need a corresponding ADR-creation discipline in
  `.project/decisions/` matching the paragraph's "the resolution
  itself is recorded as an ADR" promise; if that discipline
  drifts, the paragraph becomes stale. Worth tracking but not
  flaggable on the spec itself.

- The OQ-3 cell rewording closes flag-1 cleanly, but the same
  pattern (`<verb>bypassed</verb>` framing implying current
  behaviour) does not appear in other OQ rows. The validator
  swept OQ-1, OQ-2, OQ-4..OQ-12 for similar ambiguity and
  found none — OQ-3 was the unique offender. No new flags.

- No XML construct was touched in v6, so the conformance audit's
  surface (28 constructs C-01..C-28) is byte-identical to v5. No
  schema-namespace flags, no annex-vs-chapter mismatches.
