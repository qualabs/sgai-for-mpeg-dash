[GROUNDED_BY=spec-only]

# Spec detail review — v7 (2026-05-20)

**Review target**: `../output/v7-sgai-spec.md`.
**Reference**: `../context/` at git SHA `e3f3939` (working tree
clean re context/ — `context/` not modified by v7).
**Method**: deterministic scan across eight micro-consistency
categories; autofixes applied in place where the right answer is
unambiguous, flags reported here. Findings already captured by
`../output-analysis/v7-spec-validation.md` (the eight gaps, seven
edge cases, two ambiguities, and G-7 naming-collision on
`@earliestResolutionTimeOffset`) are not restated here. The two
v6 detail-review flags are reviewed for closure status below.

## Summary

- Autofixes applied: 0
- Issues flagged for human decision: 1
- Categories with no issues: format consistency (cat 1 — no slip);
  cross-reference consistency (cat 2 — none introduced);
  uniformity (cat 4 — newly **closed** for the OQ "Origin in this
  spec" column, see v6 flag closure below); naming reuse with
  different semantics (cat 3 — the
  `@earliestResolutionTimeOffset` collision is captured by
  validation G-7 and is on the refinement-gaps list, not re-flagged
  here); annex vs chapter mismatch (cat 5 — no annex was touched in
  v7); editorial regression on the chapter-vs-annex axis (cat 6 —
  every v7 edit is in the OQ section, which is informative, not
  annex; no propagation drift); schema-namespace pinning (cat 7 —
  no XML example was touched).

## v6 flag closure status

| v6 flag | Subject | v7 status | Notes |
|---------|---------|-----------|-------|
| #1 | OQ placeholder issue numbers | **DEFERRED** (per the flag's own recommendation) | The flag explicitly recommends deferral until the issues are created in `qualabs/sgai-for-mpeg-dash`. The issues are not yet created; deferral persists. Re-emitted below as a continuing-deferral note (not a new flag). |
| #2 | OQ "Origin in this spec" column uniformity | **CLOSED** | v7 normalises the column across all twelve OQ rows. Design-item OQs (OQ-1..OQ-8, OQ-12) all use the template `<refs> (the open question, if admitted/resolved, would <implication>)`. Process-item OQs (OQ-9, OQ-10, OQ-11) all use the template `n/a — <qualifier>` (`coordination item`, `governance item`, `paper output item`). The em-dash separator replaces v6's parenthetical form for the process rows. OQ-3 was already at the template after v6's flag-1 closure and is unchanged in v7. Each implication clause added is a plain-language paraphrase of the same row's "Open question" cell — no new substantive claim is introduced. Annotation: `<!-- refine: v6-detail-review.md#flag-2 -->` placed immediately above the table header. |

## Autofixes applied

None. v7's delta surface (one editorial sweep across the twelve OQ
"Origin in this spec" cells, one version-line bump) introduced no
new mechanical inconsistencies that an autofix would correct. The
normalisation pass required a semantic decision (which implication
clause best paraphrases each OQ row without introducing new claims;
which short qualifier best categorises each process-item row), so
it routes through delta-applied annotated edits
(`<!-- refine: v6-detail-review.md#flag-2 -->`) rather than the
autofix log.

## Flagged issues

| # | Category | Spec ref | Rule anchor | Description | Suggested resolution | Recommendation |
|---|----------|----------|-------------|-------------|----------------------|----------------|
| 1 | cross-reference consistency (cat 2) | "Open questions for WG (2026-06-02 meeting)" section, every OQ-N cell mentioning an Issue # | spec-internal: the OQ table maps OQ-1..OQ-12 to GitHub Issues #2..#13. The spec body still acknowledges the issues "are placeholders until creation". A reader following the issue # without context still cannot tell whether the issue actually exists. (Carried-forward continuation of v5 flag-2 / v6 flagged item #1.) | Once the issues are created in `qualabs/sgai-for-mpeg-dash`, update the section to (a) remove the "placeholders" qualifier and (b) optionally turn each issue # into a hyperlink to the issue URL. | Continue deferring until the issues are created. Apply a minor refinement once the GitHub state is stable. No action required in v7.x without GitHub state change. |

## Open questions surfaced

- The v7 normalisation closes the only editorial sweep that the
  minor-refinement envelope still had available after v6. The
  validator's micro-consistency scan across the remaining seven
  categories surfaces no new flag. The next residual issue that
  could be touched without a context-level change or a WG
  decision is the placeholder-issue-numbers caveat (flagged item
  #1 above), and that is gated by external GitHub state, not by
  any in-spec editorial work the agent can complete autonomously.

- The implication clauses introduced into the OQ "Origin in this
  spec" cells were cross-checked against each row's "Open
  question" cell to confirm they restate (not extend) the open
  question already on the page. Spot checks:
  - OQ-1 "would introduce a layout primitive or descriptor-by-
    reference path that R10 currently prohibits" — paraphrases
    "DASH-native layout primitives vs IAB ad-signaling by
    reference. R10 ... prohibits a parallel layout system; an
    SVTA proposal ... introduces DASH-native layout attributes
    which conflicts with R10".
  - OQ-2 "would relax the B-7 cap and require conflict-resolution
    rules across overlays" — paraphrases "R14 restricts this
    edition to one non-linear form at a time. An SVTA proposal
    ... allows multiple concurrent overlays. WG has not decided
    whether to relax R14 nor whether to define conflict-
    resolution rules".
  - OQ-4 "would introduce a new event whose resolution defers the
    linear-vs-non-linear choice to the ADS" — paraphrases "An
    SVTA proposal ... introduces a `DynamicPresentation` event
    that defers the choice between linear and non-linear to ADS
    resolution".
  - OQ-12 "would require a forward-compatibility hook in the
    carrier shape so a future edition can layer the enhancement-
    layer composition path on top" — paraphrases "Whether the OOS
    deferral is sufficient or whether the carrier shape introduced
    by this edition needs forward-compatibility hooks for the
    enhancement-layer composition path".
  No clause introduces a claim absent from its row's "Open
  question" cell.

- No XML construct was touched in v7, so the conformance audit's
  surface (28 constructs C-01..C-28) is byte-identical to v5 / v6.
  No schema-namespace flags, no annex-vs-chapter mismatches.

- The convergence runway for minor-only refinement is now empty.
  No further editorial sweep is available without either (a) a
  context-level change (the five recommendations carried over
  from v5) or (b) an external state change (the GitHub issues
  being created). The minor-refinement envelope has converged on
  v7.
