# Spec convergence — v4.1 → v4.2

Generated by `prompts/compare-spec-versions.prompt`.

## Issue counts

| Category | v4.1 | v4.2 | Δ | Trend |
|---|---|---|---|---|
| Gaps | 10 | 8 | -2 | ✅ |
| Edge cases | 7 | 7 | 0 | ➖ |
| Ambiguities | 2 | 2 | 0 | ➖ |
| R coverage — full | 11 | 11 | 0 | ➖ |
| R coverage — partial | 1 | 1 | 0 | ➖ |
| R coverage — gap | 0 | 0 | 0 | ➖ |
| R coverage — governance | 3 | 3 | 0 | ➖ |
| Autofixes applied (detail-review) | 6 | 2 | -4 | ⚠️ |
| Flagged (detail-review) | 3 | 5 | +2 | ⚠️ |
| Conforming (audit) | 22 | 22 | 0 | ➖ |
| Marginal (audit) | 5 | 5 | 0 | ➖ |
| Non-conforming (audit) | 1 | 1 | 0 | ➖ |
| Fetch-failed | 0 | 0 | 0 | ➖ |
| **Total issues** | **29** | **29** | **0** | **➖** |

Trend legend:
- ✅ — fewer issues (delta < 0) OR more conforming / coverage-full / governance / autofixes (delta > 0).
- ⚠️ — more issues OR less conforming / coverage-full / governance.
- ➖ — no change.

## Verdict

**STALLED**

Total issues are unchanged (29 → 29). Two gaps were closed at the
spec level (v4.1 G-9 rescinded as false positive; v4.1 G-10 closed
by an inline §5.1.2 NOTE), but the detail-review surface absorbed
the slack: the v4.2 detail-review pass surfaced five new editorial
flags (vs three in v4.1) while applying fewer autofixes (two vs
six in v4.1). The conformance-audit picture is byte-identical
across both runs (22 conforming, 5 marginal, 1 non-conforming),
and the structural items that gate further convergence (R13
partial coverage, N-1 zero-`<Period>` overlay resolution doc,
M-2 `@earliestResolutionTimeOffset` rename) all require
`context/` changes outside minor scope. Decision: escalate to a
major build (`v5`) to address the requirement-level blockers, or
accept v4.2 as the stable end of the minor track.

- **ON TRACK**: total issues decreased AND no category increased significantly. Continue refining (`v4.3`) if material issues remain, or stop if residual is acceptable.
- **STALLED**: total issues unchanged. Consider whether remaining issues need requirement-level changes (escalate to a major build `v5`).
- **REGRESSION**: total issues increased. Review the refined spec for unintended changes.

## Per-category notes

- **Gaps -2 (10 → 8)**: net closure of G-9 (false positive
  rescinded) and G-10 (closed by the new §5.1.2 NOTE pinning
  `@returnOffset` / `@startWithOffset` types and semantics).
  Remaining G-1..G-8 all require `context/` changes — outside
  minor refinement scope.
- **Autofixes -4 (6 → 2)**: expected. v4.1 absorbed the bulk of
  the v4-flagged trivia (six fixes spanning format, cross-ref,
  and schema-namespace categories); v4.2 had a smaller surface to
  remediate (only two: a broken §3.3 OOH cross-reference and a
  §3.1 misplaced-parent slip). Lower autofix count reflects a
  cleaner starting point, not a regression.
- **Flagged +2 (3 → 5)**: the v4.2 detail-review pass scanned
  the same surface that v4.1's pass already cleared and surfaced
  finer-grained editorial issues (three cross-reference errors in
  §3.3 / §3.2 / §2.1; one §3.2 misalignment on `image/*` MIME
  enum; one §4.3 ADS-MAY bullet contradicting §5.6's SVTA-namespaced
  carriers for `<svta:Click>` and `<svta:UniversalAdId>`). The
  newly surfaced issues are all editorial / cross-reference grade,
  not structural — fixable in another minor pass without context
  changes.
- **Conformance audit unchanged (22 / 5 / 1)**: the v4.2 spec-level
  NOTEs closed M-5 at the spec level but kept the verdict at
  Marginal (the same prose-fidelity trade-off as M-1). No new
  conformance findings; no escalations.
