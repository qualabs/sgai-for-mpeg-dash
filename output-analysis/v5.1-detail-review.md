[GROUNDED_BY=spec-only]

# Spec detail review — v5.1 (2026-05-20)

**Review target**: `../output/v5.1-sgai-spec.md`.
**Reference**: `../context/` at HEAD.
**Method**: deterministic scan across eight micro-consistency
categories; v5.1 is a minor refinement of v5, so the surface
already audited in v5's detail review carries forward. This pass
re-checks (a) the seven refinement deltas applied in v5.1 for new
issues, and (b) whether v5 flag-2, flag-3, flag-4 were resolved as
intended.

## Summary

- Autofixes applied: 0 (v5.1 is a minor refinement; the
  detail-review prompt's autofix surface is clean — no broken
  cross-refs, no missing xmlns, no annex/chapter drift introduced
  by the v5.1 deltas).
- Issues flagged for human decision: 2 (both carried forward from
  v5 — flag-1 and flag-5; v5's flag-2, flag-3, flag-4 were
  resolved by the v5.1 refinement).
- Categories with no issues: format consistency; cross-reference
  consistency; annex vs chapter mismatch; schema-namespace
  pinning; TODO/FIXME/placeholder leftovers; editorial regression.

## Autofixes applied

None. v5.1 introduces refinement deltas only; the eight
micro-consistency categories were already clean on v5 and the
deltas did not introduce new format / cross-ref / namespace
issues. Each delta is annotated inline with
`<!-- refine: ... -->` comments that markdown ignores.

## Flagged issues

| # | Category | Spec ref | Rule anchor | Description | Suggested resolution | Recommendation |
|---|----------|----------|-------------|-------------|----------------------|----------------|
| 1 | naming reuse with different semantics | §5.1.3 attribute table (`@earliestResolutionTimeOffsetTicks`) | `context/06-naming-and-namespaces.md` N06.10 / N06.11 | Carried forward from v5 flag-1. The `Ticks` suffix is the right disambiguator (concise, surfaces unit at call site) but a future minor refinement MAY prefer a verbose name. v5.1 does not regress; the choice is unchanged. | Keep `@earliestResolutionTimeOffsetTicks` for v5.1; revisit if `06-naming-and-namespaces.md` introduces a normative suffix convention. | Hold. Acceptable; tracked in Refinement gaps row 7. |
| 2 | naming reuse with different semantics | §5.5.3 NOTE on `<EventStream>` inside `<svta:RenderableAsset>` | `context/06-naming-and-namespaces.md` N06.10 / N06.11 + `context/08-dash-extension-rules.md` DR-2 / DR-3 | Carried forward from v5 flag-5. The reinterpretation pattern remains documented in §5.5.3 NOTE; v5.1 added the §5.5.4 single-rooted beacon-set tightening (A-1) but the underlying naming question is unchanged. Requires a context-level DR-8 admission or a rename. | Resolve in a future major build by adding DR-8 in `context/08-dash-extension-rules.md` or by renaming the SGAI-side tracking carrier. | Hold pending context decision. Tracked in Refinement gaps row 8. |

## Resolved (from v5)

| # | v5 flag | v5.1 resolution |
|---|---------|----------------|
| 1 | v5 flag-2 (§5.1.4 lacks an explicit NOTE on the deliberate omission of `@earliestResolutionTimeOffsetTicks`) | Resolved. v5.1 §5.1.4 adds the NOTE with explicit cross-reference to §6.2. |
| 2 | v5 flag-3 (§5.4.1 lacks an explicit citation of N06.13 for the `@duration` name reuse) | Resolved. v5.1 §5.4.1 cites `context/06-naming-and-namespaces.md` N06.13 inline. |
| 3 | v5 flag-4 (§4.4.8 + §7.6 redundant prose on the overlay's slot-window clock during pause) | Resolved. v5.1 §4.4.8 replaces the duplicated detail with a cross-reference to §7.6; §7.6 retains the canonical text. |

## Open questions surfaced

- The two flagged issues above (flag-1 unit-suffix policy,
  flag-5 / M-1 DR-8 candidate) remain context-level decisions.
  They are tracked in v5.1's Refinement gaps section (rows 7 and
  8) and are not detail-review autofix candidates.
