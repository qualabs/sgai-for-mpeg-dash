# Tasks — phase 01-setup

| id   | brief                                                     | status | plan | evidence                                                                                                                                                                       |
| ---- | --------------------------------------------------------- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-01 | Repo + workflow (layout + build pipeline + governance)    | done   | —    | `README.md`, `CLAUDE.md`, `SETUP.md`, `.env.agent.example`, `.gitignore`, `prompts/analyze-dash-gap.prompt`, `prompts/build-norm.prompt`, `prompts/build-all.prompt`, `.project/PROJECT.md`, `.project/LOG.md`, `.project/phases/01-setup/`, `.project/decisions/`. Initial commit: `1c6a954`. |
| T-02 | Initial specs to iterate on (6 files in `spec/`)          | done   | —    | `spec/01-intro.md`, `spec/02-actors.md`, `spec/03-requirements.md`, `spec/04-use-cases.md`, `spec/05-dash-linear-interfaces.md`, `spec/99-glossary.md`.                          |

Both tasks were executed during the session of 2026-05-11 and are
captured in `LOG.md` and `REPORT.md`.
